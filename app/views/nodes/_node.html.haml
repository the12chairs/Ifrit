%div.thread
  %div.head
    = node.head
    = node.formated_date
    №
    = node.number
  %div.pic= link_to( image_tag( node.picture_url(:thumb).to_s), node.picture_url) if node.picture?
  %div.text
    = parser(node.body)
  %div.foot
    Total comments:
    = node.comments.size
    = link_to 'Reply', node
    = #link_to 'Delete node', node, :confirm => 'Are you sure?', :method => :delete

- if node.comments.size == 1
  = render node.comments.last
- if node.comments.size == 2
  = render node.comments.limit(2).offset(node.comments.size - 2)
- if node.comments.size > 2
  = render node.comments.limit(3).offset(node.comments.size - 3)
%hr
